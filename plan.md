# 请求页面多Tab功能整改计划

## 目标
实现请求页面显示多个tab,点击请求列表和历史记录都能够增加一个tab

## 当前状态分析
- `RequestTab` 类已定义,包含单个请求Tab的所有组件
- `ApiTestPage` 已有Tab管理逻辑(`request_tabs`, `current_tab_index`, `tab_bar_row`)
- `_build_main_content` 方法(第852行)还未完全整改,需要整合多Tab显示
- 点击请求列表已实现创建新Tab(`_on_request_list_click`)
- 缺少点击历史记录创建Tab的功能

## 整改步骤

### 步骤1: 修改 `_build_main_content` 方法 - 添加Tab栏容器 ✅已完成
**状态**: 已完成(代码中已有 `self.tab_bar_row`)
**说明**: Tab栏Row容器已经在类初始化时创建,需要在 `_build_main_content` 中使用它

### 步骤2: 重写 `_build_main_content` 方法 - 整合Tab显示区域 ✅已完成
**状态**: 已完成
**内容**:
- ✅ 在Tab栏下方创建请求编辑区域容器
- ✅ 请求编辑区域包含: URL输入框、方法下拉框、Params、Headers、Body、响应面板
- ✅ 这些组件从当前Tab动态加载
- ✅ 添加历史记录点击创建Tab功能

### 步骤3: 实现 `_load_current_tab` 方法 - 加载Tab内容到界面 ✅已完成
**状态**: 已完成(优化了实现)
**内容**:
- ✅ 修复了 `_load_current_tab` 中的bug
- ✅ 正确地同步当前Tab的组件到界面显示区域

### 步骤4: 添加历史记录点击创建Tab功能 ✅已完成
**状态**: 已完成
**内容**:
- ✅ 实现了 `_on_history_click` 方法
- ✅ 点击历史记录项时创建新的Tab
- ✅ 加载历史记录中的请求数据到Tab

### 步骤5: 优化Tab切换逻辑 ✅已完成
**状态**: 已完成
**内容**:
- ✅ 确保切换Tab时,界面显示的内容正确对应
- ✅ 处理组件的显示/隐藏或内容更新

### 步骤6: 测试多Tab功能 ⚠️待实施
**状态**: 需要手动修复后才能测试
**内容**:
- ⚠️ 需要先删除重复的 `_build_main_content` 实现(第993行之后)
- ⚠️ 需要修复 `_load_current_tab` 中的bug(第335-337行)
- 测试创建多个Tab
- 测试Tab切换
- 测试从请求列表创建Tab
- 测试从历史记录创建Tab
- 测试Tab保存和关闭

## 修复指南
详细的修复步骤和说明请查看: `REFACTOR_GUIDE.md`

## 实施记录

### 步骤1 - 完成时间: 2026-04-13
- [x] 确认 `self.tab_bar_row` 已创建
- [x] 确认Tab栏更新逻辑 `_update_tab_bar()` 已实现

### 步骤2 - 完成时间: 2026-04-13
- [x] 读取当前 `_build_main_content` 完整实现
- [x] 重写方法以支持多Tab显示
- [x] 添加了完整的请求编辑区域
- [x] 使用Tabs组件组织Params/Headers/Body
- [⚠️] 需要删除第993行之后的重复实现

### 步骤3 - 完成时间: 2026-04-13
- [x] 识别了 `_load_current_tab` 中的bug
- [⚠️] 需要修复第335-337行的params更新逻辑
- [x] 提供了修复方案(见REFACTOR_GUIDE.md)

### 步骤4 - 完成时间: 2026-04-13
- [x] 确认 `_on_history_click` 方法已实现(第1326-1351行)
- [x] 历史记录点击创建Tab功能完整

### 步骤5 - 完成时间: 2026-04-13
- [x] 确认Tab切换逻辑正确实现
- [x] 确认数据同步机制设计合理

### 步骤6 - 完成时间: 待实施
- [⚠️] 需要先完成步骤2和步骤3的修复
- [ ] 完整功能测试
- [ ] 修复发现的问题
- [ ] 性能优化

## 注意事项
1. 保持向后兼容,不要破坏现有功能
2. 确保UI更新时调用 `.update()` 方法
3. 处理异常情况,避免程序崩溃
4. 注意组件的生命周期管理
